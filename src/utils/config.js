/** Api url for the jokes. */
const api = 'https://api.icndb.com/jokes';
/** Session key */
const sessionKey = 'SES_ID';
/** Routes  */
const route = {
  jokes: '/jokes',
  login: '/login',
};

export { api, route, sessionKey };
